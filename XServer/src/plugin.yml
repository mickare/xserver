name: XServer
main: com.mickare.xserver.XServerPlugin
version: 2.0.0
author: mickare
description: XServer Communication Interface, that plugins can use to send messages to other servers.
commands:
  xserverreload:
    description: Broadcast a Message.
    usage: /<command>
    permission: xserver
    permission-message: You don't have <permission>